CREATE DATABASE virtual_classroom;

USE virtual_classroom;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100) UNIQUE,
    password VARCHAR(100),
    role VARCHAR(20),
    status VARCHAR(20) DEFAULT 'pending'
);

CREATE TABLE classes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    class_name VARCHAR(100),
    faculty_id INT,
    class_code VARCHAR(20)
);

CREATE TABLE enrollments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_id INT,
    class_id INT
);

CREATE TABLE materials (
    id INT AUTO_INCREMENT PRIMARY KEY,
    class_id INT,
    title VARCHAR(100),
    file_path VARCHAR(255),
    video_link VARCHAR(255)
);

CREATE TABLE announcements (
    id INT AUTO_INCREMENT PRIMARY KEY,
    class_id INT,
    message TEXT,
    date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO users (name, email, password, role, status)
VALUES ('Admin', 'admin@gmail.com', 'admin123', 'admin', 'approved');

ALTER TABLE enrollments 
ADD CONSTRAINT unique_enrollment UNIQUE(student_id, class_id);

UPDATE users 
SET role='admin' 
WHERE email='admin@gmail.com';

ALTER TABLE classes 
ADD subject VARCHAR(100),
ADD section VARCHAR(50);

UPDATE classes 
SET subject='Java', section='A' 
WHERE id=1;

ALTER TABLE announcements ADD file_path VARCHAR(255);